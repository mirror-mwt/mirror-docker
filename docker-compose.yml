version: "3.3"

services:
  mirror-primary:
    container_name: mirror-primary
    build:
      context: .
      dockerfile: Dockerfile
    env_file: env.conf
    volumes:
      - /mnt/storage/mirror:/mirror
    restart: "unless-stopped"
